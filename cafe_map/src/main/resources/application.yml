server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zebra?characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: root
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

mybatis:
  mapper-locations: classpath:/mappers/**/*.xml
  type-aliases-package: com.aloha.cafe_map.domain
